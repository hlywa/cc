using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class Dater: MonoBehaviour
{
    //each button needs this script.
    //reference a boolean in here 

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //add in a function that when you press a button, it turns a boolen to off and if you press it again, you turn it to on
    // look at this syntax
    // boolean =! boolean 
}
